---
name: retro
description: Review a completed loop unit and turn measured recurring friction into audience-tagged lessons. Use when the user requests a loop retrospective.
---

Read [Codex integration rules](../../../.codex/LOOP.md), then the Markdown body of the [existing retro workflow](../../../.claude/skills/retro/SKILL.md). Both are required: the existing workflow supplies the tested stage behavior; this entrypoint adapts the harness. Ignore the source YAML frontmatter. The integration rules and substitutions below take precedence over harness-specific source text. User instructions and existing authorization take precedence over template approval checkpoints.

Follow the source workflow in the main session. Audit records retain the same role names and columns across tools. Evaluate observations within the unit, do not infer model cost from Claude model names. Check both the Codex integration rules and the original role instructions before retiring a lesson as already enforced. Keep the lesson and journal preservation rules intact.
